#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#

pre-commit:
  commands:
    prettier:
      glob: '*.{js,jsx,ts,tsx,json,css,scss,md,html,vue,svelte}' # Adjust file types as needed
      run: npx prettier --write {staged_files}
